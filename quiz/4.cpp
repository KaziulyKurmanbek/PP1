#include <iostream>
#include <cmath>
#include <algorithm>
using namespace std;
int main(){
    int n;
    cin>>n;
    string str;
    cin>>str;
    string str2;
    str2=str;
    int j=str.size();
    reverse(str2.begin(),str2.end());
    for(int i=0; i<str.size()/2; i++){
        if(str[i]==str2[j]){
            
        }
    }
}