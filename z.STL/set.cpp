#include <iostream>
#include <vector>
#include <set>
using namespace std;
int main(){
    set<int> s;
    set<int>::iterator it;   //set<int>::iterator - полноценный data type;
   //iterator does not have any number value it is inly address
   //m.find(key)
}